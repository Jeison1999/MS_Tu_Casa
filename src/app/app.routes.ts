import { Routes } from '@angular/router';
import { WelcomeComponent } from './feature/welcome-component/welcome-component';

export const routes: Routes = [

    {
        path: '',
        component: WelcomeComponent,
    }
];
